---
name: add-prompthub-ecc-bundle-command
description: Workflow command scaffold for add-prompthub-ecc-bundle-command in prompthub.
allowed_tools: ["Bash", "Read", "Write", "Grep", "Glob"]
---

# /add-prompthub-ecc-bundle-command

Use this workflow when working on **add-prompthub-ecc-bundle-command** in `prompthub`.

## Goal

Adds a new command definition for managing the prompthub ECC bundle.

## Common Files

- `.claude/commands/add-prompthub-ecc-bundle.md`

## Suggested Sequence

1. Understand the current state and failure mode before editing.
2. Make the smallest coherent change that satisfies the workflow goal.
3. Run the most relevant verification for touched files.
4. Summarize what changed and what still needs review.

## Typical Commit Signals

- Add .claude/commands/add-prompthub-ecc-bundle.md

## Notes

- Treat this as a scaffold, not a hard-coded script.
- Update the command if the workflow evolves materially.