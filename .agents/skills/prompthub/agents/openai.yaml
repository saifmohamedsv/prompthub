interface:
  display_name: "Prompthub"
  short_description: "Repo-specific patterns and workflows for prompthub"
  default_prompt: "Use the prompthub repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true